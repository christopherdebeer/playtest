{
  "name": "playtest-site",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run generate && vite",
    "build": "npm run generate && tsc -b && vite build",
    "preview": "vite preview",
    "generate": "npm run generate-games && npm run generate-mechanics && npm run generate-logs && npm run generate-docs",
    "generate-games": "node scripts/generate-games.js",
    "generate-mechanics": "node scripts/generate-mechanics.js",
    "generate-logs": "node scripts/generate-logs.js",
    "generate-docs": "node scripts/generate-docs.js"
  },
  "dependencies": {
    "marked": "^17.0.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.22.0",
    "yaml": "^2.3.0"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.2.1",
    "typescript": "^5.3.0",
    "vite": "^5.1.0"
  }
}
